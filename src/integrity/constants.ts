/** Base URL for the Microsoft Graph REST API v1.0. */
export const MS_GRAPH_BASE = "https://graph.microsoft.com/v1.0"

/** Base URL for the Microsoft Graph REST API beta (used for task list groups). */
export const MS_GRAPH_BETA_BASE = "https://graph.microsoft.com/beta"

/** Base URL for the GitHub REST API. */
export const GITHUB_API_BASE = "https://api.github.com"

/** User-Agent header value sent with every outbound HTTP request. */
export const USER_AGENT = "integrity-mcp/1.0"
